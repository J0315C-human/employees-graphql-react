{
  "name": "employees-graphql-react-project",
  "version": "0.0.0",
  "description": "a code challenge",
  "main": "index.js",
  "engines": {
    "node": ">=8.9.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon index.js localhost 4000",
    "client": "cd client && npm run start",
    "start": "npm-run-all --parallel server client",
    "postinstall": "cd client && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-datasource-rest": "^0.6.1",
    "apollo-server": "^2.25.4",
    "faker": "^4.1.0",
    "graphql": "^14.4.2"
  },
  "devDependencies": {
    "nodemon": "^1.19.1",
    "npm-run-all": "^4.1.5"
  }
}
